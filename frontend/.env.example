# Frontend environment variables — copy this file to .env.local for local dev,
# or configure the values in the Render dashboard for production.

# URL of the FastAPI backend (only needed when the frontend is deployed as a
# separate Render static site; leave empty for local Vite dev-server proxy).
VITE_API_BASE_URL=
